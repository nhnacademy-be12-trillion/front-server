

# [2] ??? ?? ?? ?? (Feign Client?)
# ??: ??? ?? ??? 8081? ??? ? ?????!
cart.api.url=http://localhost:10407

gateway.url=http://localhost:10400

server.port=${SERVER_PORT:10402}

# ==============================================================================
# REDIS CONFIGURATION
# ==============================================================================

# 1. ?? ??
spring.data.redis.host=localhost
spring.data.redis.port=6379
# spring.data.redis.password=  <-- ???? ????? ?? ?? ? ??
spring.data.redis.database=0

# 2. ???? ?? (MSA?? ??: ?? ?? ?????? ? ?)
spring.data.redis.timeout=5000ms
spring.data.redis.connect-timeout=5000ms

# 3. ??? ? ?? (Lettuce & Commons Pool2)
# - ?? ??? ? (???? ?? ??, ??? 8? ?? ??)
spring.data.redis.lettuce.pool.max-active=20
# - ???? ? ?? ??? ?? ??? ?
spring.data.redis.lettuce.pool.min-idle=5
# - ??? ??? ? ?? ?? ?? (? ???? ?? ??)
spring.data.redis.lettuce.pool.max-wait=1000ms

# ==============================================================================
# SPRING CACHE CONFIGURATION
# ==============================================================================

# ?? ??? Redis? ??
spring.cache.type=redis
# (??) null ?? ???? ?? (?? ??)
spring.cache.redis.cache-null-values=false
# (??) ?? ?? TTL (Time To Live) - ?? ? ?? ??? ?? (1?? ??)
# ??? ??? Java Config?? ?? ???? ??? ???? ?? '???'???.
spring.cache.redis.time-to-live=60m

# ?? ???? ?? ??? ?
app.cookie.secret="v3rys3cr3tK3yF0rNHNAcademyPr0j3ctD0N0tSh4r3!!"


# ==============================================================================
# THYMELEAF CONFIGURATION
# ==============================================================================
# 개발 시 캐시 끄기 (수정 후 F5 누르면 바로 반영되도록)
spring.thymeleaf.cache=false
# HTML 경로 접두사/접미사 (기본값이지만 명시적으로 확인)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
# 렌더링 모드
spring.thymeleaf.mode=HTML
# 인코딩
spring.thymeleaf.encoding=UTF-8


# ==============================================================================
# LOGGING CONFIGURATION
# ==============================================================================
# 1. 우리 프로젝트 로그 레벨 (DEBUG로 해야 캐시 히트 로그 보임)
logging.level.com.nhnacademy=DEBUG

# 2. Feign 통신 로그 (어떤 요청이 나가는지 확인)
logging.level.com.nhnacademy.frontserver.cart.client=DEBUG

# 3. openfeign 로깅 레벨 (개발 중엔 FULL로 요청/응답 다 보는 게 좋음)
spring.cloud.openfeign.client.config.default.logger-level=FULL

# 3. (선택) Redis 쿼리 나가는 거 보고 싶으면
# logging.level.io.lettuce.core=DEBUG

toss.client.key = ${TOSS_CLIENT_KEY}